# Local development database. Match at any depth: this file was briefly
# committed because the ignore pattern was path-anchored while the db sat at
# the repo root.
local.db
local.db-wal
local.db-shm
*.sqlite
*.sqlite3

__pycache__/
*.py[cod]
.env
.env.*
.venv/
venv/
.DS_Store

# Held out of the index until the git credential has the "workflow" scope.
# Canonical copies live in topic-radar/deploy/ — see the README there.
.github/
