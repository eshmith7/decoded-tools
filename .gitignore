# Local development database. Match at any depth: this file was briefly
# committed because the ignore pattern was path-anchored while the db sat at
# the repo root.
local.db
local.db-wal
local.db-shm
*.sqlite
*.sqlite3

__pycache__/
*.py[cod]
.env
.env.*
.venv/
venv/
.DS_Store

# Agent worktrees, created by the harness. They are checkouts of this same
# repository and must never be committed into it.
.claude/
